"""API entry point — scaffold only (Fase 4 implements FastAPI app)."""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError(
        "API no implementada aún. Fase 4: FastAPI + webhook Twilio. "
        "El bot activo sigue en la raíz: python run.py"
    )


if __name__ == "__main__":
    main()
